<Window x:Class="VaultMessenger.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="VAULT Messenger - Login" 
        Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        TextElement.Foreground="White"
        FontFamily="Segoe UI">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Logo and Title -->
        <StackPanel Grid.Row="0" Margin="0,40,0,20" HorizontalAlignment="Center">
            <Viewbox Height="80" Width="80">
                <materialDesign:PackIcon Kind="ShieldLock" Foreground="{StaticResource PrimaryBrush}"/>
            </Viewbox>
            <TextBlock Text="VAULT" FontSize="36" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,10,0,0"
                       Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock Text="Military-Grade Secure Messaging" FontSize="12" 
                       HorizontalAlignment="Center" Margin="0,5,0,0"
                       Foreground="#999" FontStyle="Italic"/>
        </StackPanel>

        <!-- Login Form -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" 
                CornerRadius="20,20,0,0" Padding="40">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Sign In" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,30"/>
                
                <!-- Username -->
                <TextBox x:Name="UsernameTextBox"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         materialDesign:HintAssist.Hint="Username or Email"
                         FontSize="14" Margin="0,0,0,20"/>
                
                <!-- Password -->
                <PasswordBox x:Name="PasswordBox"
                            Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                            materialDesign:HintAssist.Hint="Password"
                            FontSize="14" Margin="0,0,0,10"/>
                
                <!-- Forgot Password -->
                <TextBlock Text="Forgot Password?" 
                          Foreground="{StaticResource SecondaryBrush}"
                          HorizontalAlignment="Right"
                          Margin="0,0,0,30"
                          Cursor="Hand"
                          MouseLeftButtonDown="ForgotPassword_Click"/>
                
                <!-- Login Button -->
                <Button x:Name="LoginButton"
                        Content="LOGIN"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="White"
                        Height="45" FontSize="14" FontWeight="SemiBold"
                        Click="LoginButton_Click"
                        Margin="0,0,0,15"/>
                
                <!-- Error Message -->
                <TextBlock x:Name="ErrorTextBlock"
                          Foreground="{StaticResource ErrorBrush}"
                          TextWrapping="Wrap"
                          Visibility="Collapsed"
                          Margin="0,0,0,15"
                          TextAlignment="Center"/>
                
                <!-- Register Link -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Don't have an account? " Foreground="#999"/>
                    <TextBlock Text="Sign Up" 
                              Foreground="{StaticResource SecondaryBrush}"
                              FontWeight="SemiBold"
                              Cursor="Hand"
                              MouseLeftButtonDown="SignUp_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
